# This is just a basic http server created for learning purposes

The programm takes two (2) arguments **-rootDir** which is the directory from which the files will be served, and **-port** (default is 9090)
